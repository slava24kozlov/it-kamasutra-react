import React from 'react';
import m from './Music.module.css';

const Music = (props) => {
    return (
        <div className={m.main}>
            Music
        </div>
    );
}

export default Music;
