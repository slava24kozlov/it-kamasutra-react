import React from 'react';
import n from './Dialogs.module.css';

const Dialogs = (props) => {
    return (
        <div className={n.main}>
            Dialogs
        </div>
    );
}

export default Dialogs;
